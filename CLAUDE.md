# CLAUDE.md

이 파일은 Claude Code가 세션 시작 시 자동으로 읽습니다. 이 저장소에서 실행되는
**정기 루틴(미국 주식 브리핑 등)**에 매번 적용되는 운영 규칙을 정의합니다.

---

## 미국 주식 브리핑 루틴 — 필수 가드레일

과거 두 가지 오류가 있었습니다: ① 데이터 소스 도구(UsStockInfo)가 아직 연결 중인데
너무 일찍 "미제공"으로 단정하고 전 종목을 웹으로 폴백함, ② 기준일(직전 거래일)을
가정하고 그 날짜로 검색해 **하루 어긋난 종가**를 채움. 아래 절차로 재발을 방지합니다.

### 0단계 · 사전점검 (반드시 본작업 전에 수행)

**A. 데이터 소스 도구 준비 (UsStockInfo 미노출 방지)**
- MCP 서버가 "still connecting" 상태이면, `ToolSearch`로 `UsStockInfo`
  (`get_historical_stock_prices` / `get_stock_info` / `get_finance_news`)를
  **최소 2~3회 재시도**해 로드한다. 한 번 실패로 폴백을 결정하지 않는다.
- 로드되면 티커 1개로 `get_historical_stock_prices`를 **카나리 호출**해
  실제 데이터 수신을 확인한 뒤에만 본작업을 시작한다.
- 웹 폴백은 **도구가 확실히 죽은 경우에만**, 그리고 **실패한 종목 단위로만**
  수행한다. "도구가 안 뜬다 → 전부 웹"은 금지.
- 최종 산출물 상단에 사용한 가격 출처(UsStockInfo vs 웹)를 명시한다.

**B. 기준일 도출 — 가정 금지 (하루 어긋남 방지)**
- 기준일을 미리 가정하지 않는다. 지수 1개의 `get_historical_stock_prices(period="5d",
  interval="1d")` 시계열에서 **완결된 마지막 봉의 Date를 그대로 기준일로 사용**한다.
- 일간 등락률은 **마지막 두 봉(직전 종가 대비)** 으로만 계산한다.
- 도출한 기준일이 오늘(`currentDate`) 기준 **직전 미국 정규장 거래일**과 일치하는지
  대조한다. 불일치하면 **중단하고 원인을 규명**한 뒤 진행한다.

**C. 웹 폴백 검색 규칙**
- 검색어에 특정 날짜를 넣지 않는다. `"TICKER stock close [특정날짜]"`(❌) 대신
  `"TICKER latest close / stock price"`(✅)로 검색하고, **결과에 적힌 날짜를 읽어**
  채택한다. 그 뒤 기준일과 일치하는지 반드시 교차 확인한다.

### 발행 전 검증 게이트 (Notion 저장·KakaoTalk 발송 직전)
- 표의 **모든 종가의 Date == 도출한 기준일**인지 확인한다. 하나라도 다르면
  저장·발송하지 않고 먼저 바로잡는다.
- 확인되지 않은 가격은 추정하지 말고 **"미확인"** 으로 표기한다.
- Notion 항목 제목은 기준일(종가 날짜) 규칙을 따르고, 기존 항목과 중복/오기입이
  없는지 확인한다.

### 원칙
- 가격은 **반드시 UsStockInfo 또는 웹 검색**에서만 취득한다. 추정 금지.
- 미국 증시 휴장일이면 휴장 사유만 기재한다.
- 뉴스는 이벤트 발생일 기준으로 날짜를 라벨링한다(공시일과 이벤트일이 다르면 이벤트일 기준).
