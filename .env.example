# 한국도로공사 공공데이터 API 키 (data.ex.co.kr)
# 미지정 시 "test" 키 사용 (일일 호출량 제한)
EX_API_KEY=test

# (선택) 사내 HTTP 프록시
# HTTP_PROXY=http://proxy.company.com:8080

# (선택) 사내 MITM 프록시로 TLS 인증서 검증이 실패할 때만 1 로 설정
# ALLOW_INSECURE_TLS=1
